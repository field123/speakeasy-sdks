/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { DisplayPrice, DisplayPrice$ } from "./displayprice";
import { Price, Price$ } from "./price";
import { Timestamps, Timestamps$ } from "./timestamps";
import { z } from "zod";

export type OfferingPlanMeta = {
    price?: Record<string, Price> | undefined;
    displayPrice?: DisplayPrice | undefined;
    /**
     * The owner of a resource, either `store` or `organization`.
     */
    owner: string;
    timestamps: Timestamps;
};

/** @internal */
export namespace OfferingPlanMeta$ {
    export type Inbound = {
        price?: Record<string, Price$.Inbound> | undefined;
        display_price?: DisplayPrice$.Inbound | undefined;
        owner: string;
        timestamps: Timestamps$.Inbound;
    };

    export const inboundSchema: z.ZodType<OfferingPlanMeta, z.ZodTypeDef, Inbound> = z
        .object({
            price: z.record(Price$.inboundSchema).optional(),
            display_price: DisplayPrice$.inboundSchema.optional(),
            owner: z.string(),
            timestamps: Timestamps$.inboundSchema,
        })
        .transform((v) => {
            return {
                ...(v.price === undefined ? null : { price: v.price }),
                ...(v.display_price === undefined ? null : { displayPrice: v.display_price }),
                owner: v.owner,
                timestamps: v.timestamps,
            };
        });

    export type Outbound = {
        price?: Record<string, Price$.Outbound> | undefined;
        display_price?: DisplayPrice$.Outbound | undefined;
        owner: string;
        timestamps: Timestamps$.Outbound;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, OfferingPlanMeta> = z
        .object({
            price: z.record(Price$.outboundSchema).optional(),
            displayPrice: DisplayPrice$.outboundSchema.optional(),
            owner: z.string(),
            timestamps: Timestamps$.outboundSchema,
        })
        .transform((v) => {
            return {
                ...(v.price === undefined ? null : { price: v.price }),
                ...(v.displayPrice === undefined ? null : { display_price: v.displayPrice }),
                owner: v.owner,
                timestamps: v.timestamps,
            };
        });
}
