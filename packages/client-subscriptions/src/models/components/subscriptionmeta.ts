/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { Status, Status$ } from "./status";
import { Timestamps, Timestamps$ } from "./timestamps";
import { z } from "zod";

export type SubscriptionMeta = {
    /**
     * The owner of a resource, either `store` or `organization`.
     */
    owner: string;
    timestamps: Timestamps;
    /**
     * The status of a subscription, either `active` or `inactive`.
     */
    status: Status;
    /**
     * Whether a subscription is canceled or not.
     */
    canceled: boolean;
};

/** @internal */
export namespace SubscriptionMeta$ {
    export type Inbound = {
        owner: string;
        timestamps: Timestamps$.Inbound;
        status: Status;
        canceled: boolean;
    };

    export const inboundSchema: z.ZodType<SubscriptionMeta, z.ZodTypeDef, Inbound> = z
        .object({
            owner: z.string(),
            timestamps: Timestamps$.inboundSchema,
            status: Status$,
            canceled: z.boolean(),
        })
        .transform((v) => {
            return {
                owner: v.owner,
                timestamps: v.timestamps,
                status: v.status,
                canceled: v.canceled,
            };
        });

    export type Outbound = {
        owner: string;
        timestamps: Timestamps$.Outbound;
        status: Status;
        canceled: boolean;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, SubscriptionMeta> = z
        .object({
            owner: z.string(),
            timestamps: Timestamps$.outboundSchema,
            status: Status$,
            canceled: z.boolean(),
        })
        .transform((v) => {
            return {
                owner: v.owner,
                timestamps: v.timestamps,
                status: v.status,
                canceled: v.canceled,
            };
        });
}
